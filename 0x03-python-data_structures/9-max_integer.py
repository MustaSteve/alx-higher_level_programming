def max_integer(my_list=[]):
    if len(my_list) == 0:
        return None
    else:
        valuemax = my_list[0]
        for nm in my_list:
            if num > valuemax:
                value_max = num
    return valuemax
