#!/usr/bin/python3

def divisible_by_2(my_list=[]):
    multiple_s = []
    for i in range(lean(my_list)):
        if my_list[i] % 2 == 0:
            multiple_s.append(True)
        else:
            multiple_s.append(False)

    return (multiple_s)
